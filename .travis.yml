language: cpp

compiler:  
  - gcc
  - clang

os:
  - linux
#  - osx

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake

before_script:  
  - mkdir build
  - cd build
  - cmake ..

script: make 
